import React from 'react';


// TODO: new patient form that creates a new patient with the following input:
// - name
// - riskProfile (choice of values in enum RiskProfile)
const AddPatient: React.FC = () => {
  return (
    <div>
      <h2>Add Patient</h2>
      {/* Form goes here */}
    </div>
  )
}

export default AddPatient;
